import React from "react";
import Contact from "../contact/Contact";

export default function Profile() {
  // Open source / GitHub profile is disabled; show the Contact section
  return <Contact />;
}
